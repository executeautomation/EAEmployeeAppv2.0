{
  "//": "Use this when running the app for automated UI tests. Disables Google reCAPTCHA + bot-detection on /Account/Login so Playwright/curl can sign in. Start the app with: dotnet run --environment Test",

  "ConnectionStrings": {
    "DefaultConnection": "DataSource=app.test.db;Cache=Shared"
  },

  "BotDetection": {
    "Enabled": false
  },

  "GoogleRecaptcha": {
    "SiteKey": "test-site-key",
    "SecretKey": "test-secret-key"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
